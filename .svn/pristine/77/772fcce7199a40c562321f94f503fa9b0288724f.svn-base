package com.skl.cloud.common.func;

/**
 * 转换函数.
 * <p>
 * 普遍用于一些集合和数组转换.
 * </p>
 * 
 * @author Niufei
 * 
 * @param <F>
 *            输入内容
 * @param <T>
 *            转换后内容
 */
public interface Function<F, T> {

	/**
	 * 转换
	 * 
	 * @param input
	 * @return
	 */
	T apply(F input);
}
